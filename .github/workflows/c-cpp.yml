  
name: CMake and sanitizers

on: [push, pull_request]

jobs:
  ctestsanitizers:
    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v1
    
    - name: Download g++-11
      run: sudo apt install g++-11 libboost-all-dev -y
      
    - name: Create build folder
      run: mkdir build
    - name: Run cmake
      working-directory: build
      run: cmake ..
      env:
        CXX: g++-11
    - name: Compile
      working-directory: build
      run: make
    - name: Run tests and sanitizer
      working-directory: build
      run: ctest -V
